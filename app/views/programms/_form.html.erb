<%= form_with(model: programm, local: true) do |form| %>
  <% if programm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(programm.errors.count, "error") %> prohibited this programm from being saved:</h2>

      <ul>
      <% programm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
