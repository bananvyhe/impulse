<p id="notice"><%= notice %></p>

<h1>Библиотека</h1>
<table>
<!--   <thead>
    <tr>
      <th>Name</th>
      <th>Cover</th>
      <th>Desc</th>
      <th>File</th>
      <th colspan="3"></th>
    </tr>
  </thead>
   --><tbody>
    <% @libraries.each do |library| %>
      <tr>
        <td><%= image_tag(library.cover.thumb.url) %></td>
        <td><%= library.name %></td>
        
        <td><%= library.desc %></td>
        <td><%= library.file %></td>
        <td><%= link_to 'Show', library %></td>
        <td><%= link_to 'Edit', edit_library_path(library) %></td>
        <td><%= link_to 'Destroy', library, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to 'Новая публикация', new_library_path %>
